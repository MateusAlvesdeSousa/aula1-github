package programaExercicios;

import java.util.Scanner;

import entidade.Retangulo;

public class Programa {

	public static void main(String[] args) {

		Scanner teclado = new Scanner(System.in);
		
		Retangulo retangulo = new Retangulo();
		
		System.out.println(("Informe as dimensões do Retângulo: "));
			retangulo.width = teclado.nextDouble();
			retangulo.height = teclado.nextDouble();
		
		System.out.printf("Area do retângulo é: ", retangulo.area());
		System.out.printf("O perimetro do retângulo é: ", retangulo.perimetro());
		System.out.printf("A diagonal do retângulo é: ", retangulo.diagonal());
		
		teclado.close();

	}

}
